name: hotel_smart_app
description: StayEase — Unified Smart Hotel Guest App.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '^3.10.1'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  google_fonts: ^6.1.0
  shared_preferences: ^2.3.3
  mobile_scanner: ^6.0.0
  intl: ^0.19.0
  geolocator: ^13.0.2
  geocoding: ^3.0.0
  camera: ^0.11.0
  image_picker: ^1.1.2
  video_player: ^2.9.2
  path_provider: ^2.1.4
  record: ^6.2.0
  audioplayers: ^6.1.0
  permission_handler: ^11.3.1
  firebase_core: ^4.7.0
  cloud_firestore: ^6.3.0
  url_launcher: ^6.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
